function newSelectBox() {
	var stopsContainer = document.getElementById('stops-container');
	var stopsLen = stopsContainer.childNodes.length;
	var newDiv = documnet.createElement('label');
	console.log(stopsLen);
}